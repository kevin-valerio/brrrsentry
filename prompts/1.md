Implement differential fuzzer between X and Y

---

You need to build a differential fuzzer based on the draft below while respecting the following instructions.

Using `gosentry` and its features is OBLIGATORY.

It must be a Go vs Go or Go vs X harness (where X is a language like Rust or C++).
- Use `gosentry`. Read `/home/kevinv/gosentry/README.md` first; it contains the full instructions for fuzzing with our internal tool, gosentry.
- Depending on the target, you may choose whether to detect leaks and/or race conditions; this is documented in the gosentry README.
- It can also be "itself vs itself". You are free to choose the implementation.

In-process requirement (coverage-guided)
- We want in-process fuzzing. At least one target must run in-process.
- The second target may also run in-process (preferred, if it can be included directly), or it can be a CLI target—your choice.
- We need coverage guidance, so both targets cannot be CLI-only.
- Example: if the second target is Rust, it can be run as a CLI, as long as the other target runs in-process and provides coverage feedback.
- Sometimes there is ONE target, sometimes many. For example, we can differentially fuzz multiple targets at the same time. That is tolerated as long as we have one in-process, coverage-guided target.

Bug quality (reproducibility and realism)
- The harness must be reproducible from an attacker / bug-report perspective.
- We care about SECURITY bugs, meaning bugs that can have security impact. Adapt the harness accordingly.
- The harness must NOT target an unrealistic "deep internal" path that cannot be triggered through real usage; treat that as potentially non-actionable.
- For example, if you fuzz `JSON.parse()`, that's good, but if you fuzz `JSON.getU8()`, that's not good because that is most likely a useless function and not useful for an attacker.

Grammar-based fuzzing
- Build a grammar fuzzer and define an appropriate grammar for the target.
- Review the `gosentry` grammar tutorial/prompt to ensure the grammar is correct for our target.
- The grammar must fit the target.
- You can use the oracle (safe target) end-to-end tests, integration tests, or other "high-level" tests to understand what a harness should look like or what workflow / pipeline we are interested in fuzzing.

Error handling: panic on errors
- Enable "panic on errors" (and critical errors), for example by using `--panic-on`, so issues are caught during fuzzing.
- Identify the relevant logging/error functions and configure panics in the bash script as described in the gosentry README.
- Example levels: `LvlCrit = 0`, `LvlError = 1`, `LvlWarn = 2`, `LvlInfo = 3`, `LvlDebug = 4`, `LvlTrace = 5`. In this example, we should panic on `LvlCrit` and `LvlError`.

Initial corpus
- The initial corpus is important, and it needs to be diversified. For example, we do not want 2 entries in the initial corpus that are alike, but we do want a lot of diversity.
- For this, it might be interesting to look at the project specs and take each case (or, for example, 1 feature = 1 corpus entry) so that we cover many different cases.
- You need to come up with different corpus ideas and generate them so that we can start with a strong corpus.
- You can also generate the grammar based on this corpus, or the other way around, depending on which one has the highest quality.
- Access the internet if you need to do some research.

Fuzzing campaign script
- Create a `fuzz.bash` script to start a fuzzing campaign.
- I should be able to run it like this: `CORES=0,1,2,3,4,5 ./fuzz.bash`.

VERY IMPORTANT:
- Most bugs appear in the first minutes. If you hit a crash that is not a harness issue, assume it may be a real target bug. Investigate the cause and report it to me.
- Avoid targets that would involve sequencer key compromise, buggy admin-only paths, or admin-only bugs/targets.
- Each time you find a bug or an issue that is NOT a harness or fuzzing campaign issue (so, a bug found by the fuzzing campaign), save it into a file called `FOUND_ISSUES.md`.
- Before implementing, ask me any questions you need. This can include questions about the harness or `gosentry`. If you have any doubt about what we want to do, just ask me before implementing.
- Investigate which target is the source of truth (oracle), if any, and which one is the "weakest" target to test.
- The following draft is an idea. You are free to implement a harness that can find critical bugs. If you want to deviate from the draft, you can. This is just a high-level idea I am giving you.
- The most important thing is to find potential bugs that would affect system integrity.
- If you need to understand the system, you can also use internet search.
- If you need to make glue code (libraries, etc.) to fuzz the targets properly, because for example they are in different languages, you can do it. Do not be afraid to try things.
- Create a `FUZZ.md` that contains everything you did so that we do not forget what has been done and what the fuzzing campaign is about. That will be used as a "memory" system.
- When you generate the grammar, when you generate the harness, please make sure it makes sense across all the targets. Especially use the test cases, because they might be interesting, and the high-level test, in the sense that the bugs that we found must be security concerns. I don’t want you to create a harness that might find a bug that wouldn’t necessarily be a security issue. I want you to create a harness where, if a bug is found, we’re sure it’s a security issue and that it can pose vulnerabilities.

<draft>

</draft>
