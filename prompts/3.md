Create ANY differential harness with gosentry (grammar)
---

Create ANY harness with gosentry (grammar) on the current folder and find good target to do differential fuzzing against.

I want to run a grammar-based differential fuzzing campaign on the Go application in the current directory, using gosentry (coverage-guided via LibAFL).
We will fuzz using gosentry at /home/kevinv/gosentry/README.md

Constraints from gosentry:
	•	Grammar fuzzing uses LibAFL Nautilus: inputs are generated/mutated from a user-provided Nautilus JSON grammar.  ￼
	•	Grammar mode works best with a single fuzz input argument of []byte or string.  ￼
	•	The harness should parse/convert the grammar-generated bytes/strings into domain values; grammar mode still produces bytes/strings.  ￼

Goal:
	•	Find bugs by comparing behaviors (differential fuzzing) and by checking strong oracles. A bug can include crash, panic, hang/timeouts, resource blowup, state divergence, mismatched return codes, mismatched acceptance decisions, inconsistent hashes/roots, proof disagreements, and other app-specific invariants. It needs to be application specific, with the invariants of the app, its' app depending

Later, we may use internet research to find specs, alternative implementations, and known invariants once we decide the exact target surface.
We can use the internet to know what we want to fuzz.

Basically if I TLDR.

I want to run a grammar-based differential fuzzing campaign on the current folder using `/home/kevinv/gosentry`. The goal is to find bugs in this app, so we need a reliable oracle (a reference implementation or baseline behavior to compare against).

Please propose:
	•	A good oracle we can compare the current app (harness you can make) behavior with.
	•	What we can fuzz using grammar fuzzing (not just encodings or parser-only inputs). The campaign should be “deep,” exercising real state transitions and logic.

Provide three harness ideas (what to fuzz) and, for each one:
	•	What we compare the result to (the oracle / base reference).
	•	How the differential check works (what outputs/side effects must match).

We can use the internet to research this later, but the core requirement is: differential fuzzing with a grammar.

For example in a classic myjson-lib, we would do a grammar for the JSON, and compare it against state of the art json library (written in whatever language).
Provide Github links if possible.
