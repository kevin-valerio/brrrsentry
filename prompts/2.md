Fuzz harness grammar NOT differential Gosentry

----

You are a security engineer specializing in fuzzing. You need to build a a  fuzzer harness  based on the draft below WHILE respecting the following instructions

Using `gosentry` and its features is OBLIGATORY.
It must be a Go vs Go or Go vs X (X being a language like Rust or C++) harness.
	•	Use `gosentry`. Read `/home/kevinv/gosentry/README.md` first; it contains the full instructions for fuzzing with our internal tool, gosentry.
	•	Depending on the target, you may choose whether to detect leaks and/or race conditions that's documented in gosentry README.
	- It can also be "itself vs itself". You are free to choose the implementation.
In-process requirement (coverage-guided)
	•	We want in-process fuzzing. At least one target must run in-process.
Bug quality (reproducibility and realism)
	•	Harness must be reproducible from an attacker/bug-report perspective.
	•	Harness can NOT target an unrealistic “deep internal” path that cannot be triggered through real usage, treat it as potentially non-actionable.
	 For example if you fuzz JSON.parse(), that's good, but if you fuzz JSON.getU8() that's not good because that's most likely a useless function not usefull attacker for an attacker
Grammar-based fuzzing
	• Build a grammar fuzzer and define an appropriate grammar for the target.
	• Review the `gosentry` grammar tutorial/prompt to ensure the grammar is correct for our target.
	• The grammar must fit the target
Error handling: panic on errors
	•	Enable “panic on errors” (and critical errors), e.g., using --panic-on, so issues are caught during fuzzing.
	•	Identify the relevant logging/error functions and configure panics in the bash script as described in the gosentry README. Do it if appliable
	•	Example levels:LvlCrit = 0, LvlError = 1, LvlWarn = 2, LvlInfo = 3,	LvlDebug = 4, LvlTrace = 5,	In this example, we should panic on LvlCrit and LvlError. Do it if applyable.
Fuzzing campaign script
	•	Create a fuzz.bash script to start a fuzzing campaign.
	•	I should be able to run it like: CORES=0,1,2,3,4,5 ./fuzz.bash.


VERY IMPORTANT:
- Most bugs appear in the first minutes. If you hit a crash that is not a harness issue, assume it may be a real target bug. Investigate the cause and report it to me.
- Avoid targets that would involve sequencer key compromise / buggy or admin-only bugs/targets
- Each time you have a bug or an issue that is NOT a harness or fuzzing campaign issue (so a bug from the fuzzing campaign), save it into a file called `FOUND_ISSUES.md`
- Before implementing, ask me any questions you need. It can include the harness OR gosentry.
- The following draft is an idea. You are free to implement a harness that can find critical bugs. If you want to deviate from the draft, you can, this is just an high abstraction idea I'm giving you.
- The most important is to have potential bugs that we found that would trigger system integrity.
- If you need to understand the system you can also use internet search.

<draft>
ok so the fuzzer is straight forward, i want you to do a Go IR fuzzer
you must use all the IR we already have as initial seed
and you must create an accurate IR grammar (Go intermediate represntation)
then you must fuzz it and maybe catch bugs (which ones ? i don't know, on you to check this)
</draft>
